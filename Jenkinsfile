pipeline {
    agent any
    stages {
stage('testing pipeline'){
          steps{
      echo 'test2'
                sh 'mkdir from-jenkins'
                sh 'touch from-jenkins/test.txt'
                }
        }
}
}
